services:
  application:
    image: ${TAG_API_IMAGE}
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require
    ports:
      - "8000:80"
    networks:
      - default
